/*if collision_point(x, y, pre_fight_hero, false, false){
    var targ = collision_point(x, y, pre_fight_hero, false, false)
    if data.type = "stat_up"{
        for (var i = 0; i < array_length(data.effect); i++){
            //show_message(targ.reff)
            //targ.reff.variables[$ (data.effect[i][0])] += data.effect[i][1]}
            
    }
}